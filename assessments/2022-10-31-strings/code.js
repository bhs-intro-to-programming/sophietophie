/*
 * You will need this function for some of the problems. It returns a random
 * number between 0, inclusive, and n, exclusive. Thus rand(3) could possibly
 * return 0, 1, or 2 but will not return 3.
 */
const rand = (n) => Math.floor(Math.random() * n);

// Write your code here. Remember that you get partial credit for a
// syntactically correct function skeleton and more credit for a skeleton with a
// reasonable argument list. To get full credit you need to write a correct
// function but you can get partial credit for a function that is basically
// correct even if contains small mistakes.

const firstHalf = (s) => {
  return s.substring(s.length(0)/2;
}
const secondHalf = (s) => {
  return s.substring(s.length/2);
}
const upDown = (s) => {
  return s.substring(s.toUpperCase(0)) + s.substring(s.toLowerCase(/2))
}
const firstFewEveryOther = (s) => {
  return s.substring(0, 2, 4);
}
const upDownLastCharacter = (s) => {
  return s.substring(s.toUpperCase(1)) + s.substring(s.toLowerCase(-1));
}
const firstAndLast = (s) => {
  return s.substring(0) + s.substring(-1);
}
const swapFrontAndBack = (s) => {
  return s.substring(-1) + s.substring(0);
}
const simplePigLatin = (singleWord, numFirstVowel) => {
  return s.substring(numFirstVowel)
}
const randomCharacter = (s, rand) => {
s = (rand)
const rand = (n) => Math.floor(Math.random() * n);
}
const randomCharacterUpDown = ()

const isAllUpperCase = (s) => {
  return s.toUpperCase||!s.toUpperCase;
}

